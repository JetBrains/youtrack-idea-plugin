apply plugin: 'java'

sourceCompatibility = 1.8
targetCompatibility = 1.8

jar {
    baseName = "youtrack-plugin-api"
}

dependencies {
    // any IDE/plugin classpath should contain it already
    compileOnly 'org.jetbrains:annotations:13.0'
}